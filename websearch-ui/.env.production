# Embedded build: the UI is served from the same origin as the API (at /ui/),
# so use relative paths. api.ts prepends the concrete route (/api/search,
# /admin/collections, /collections/:c/search). Leave this empty.
VITE_API_URL=
